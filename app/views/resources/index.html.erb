<h1>Resources</h1>
<table class="table" id="resources">
  <tr>
    <th>Name</th>
    <th>Capacity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @resources.each do |resource| %>
      <tr>
        <td><%= resource.name %></td>
        <td><%= resource.capacity %></td>
        <td><%= link_to 'Show', resource %></td>
        <td><%= link_to 'Update', edit_resource_path(resource) %></td>
        <td><%= link_to 'Destroy', resource, method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'Create a new Resource', new_resource_path %><br> <br>

<div class="navigation-left" style="float:right">
 <%= link_to 'Continue with Jobs', jobs_path %> &#8658;    </div><br>

<div class="navigation-left" style="float:left">
  &lArr; <%= link_to  'Go to Main menu', current_user %>    </div><br>
<div class="nagigation-left" style="float:left">
  &lArr; <%= link_to  'Go to Update menu', current_user %>    </div>



